# Скопіюйте у `.env` у цій же теці (analytics-service).
# GA4 → Admin → Data streams → ваш Web stream → Measurement Protocol API secrets

PORT=5007
GA4_MEASUREMENT_ID=G-FMBQ9260H0
GA4_API_SECRET=замініть_на_секрет_зі_GA4

# Опційно, якщо database TCP не на localhost:5001
# DATABASE_MICROSERVICE_HOST=localhost
# DATABASE_MICROSERVICE_PORT=5001
