import React from 'react';
import cl from "./CountControl.module.scss";

const CountControl = () => {
    return (

        <button className={cl.counterBtn} >-</button>
    );
};

export default CountControl;