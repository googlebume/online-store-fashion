import React from 'react';
import CountControl from './UI/CountControl/CountControl';

const Counter = () => {
    return (
        <div>
            <CountControl />
            <CountControl />
        </div>
    );
};

export default Counter;